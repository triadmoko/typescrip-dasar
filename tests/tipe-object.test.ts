describe('Tipe Object', () => {
      it('should object', () => {
            const person: { id: String, name: String } = {
                  id: "2",
                  name: "asdf"
            }
            console.log(person);

      });
});