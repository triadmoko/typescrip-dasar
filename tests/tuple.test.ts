describe('Type Data Tuple', () => {
      it('Support tuple', () => {
            const person:readonly[String, Number, Boolean]= ["df",21, true]
            console.log(person);
            
      });
});